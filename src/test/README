To run the test, it is best to compile ``emwind'' with

CFLAGS += -DNDAEMON

The other debugging flag,  CFLAGS += -DDEBUG, can be set additionally,
but it is not necessary.

Then

% make
 
Modify qrunner.sh as the output instructs, and finally

% ./runtest.sh
