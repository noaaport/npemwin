#
# $Id$
#
# Configuration file for npemwind running as an ordinary user.
# This file can be saved as
#
#	site/npemwind.conf

# The commands equivalent to the following must be executed
# (with the correct user:group)
#
#	chown -R user:group /var/npemwin
#	install -d -m 755 -o user -g group /var/run/npemwin

# user and group to run as
set user npemwin
set group npemwin

set pidfile /var/run/npemwin/npemwind.pid
