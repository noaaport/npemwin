#
# $Id$
#

By default, npemwind runs as the user/group "noaaport:noaaport". While the
package installation creates the various directories with the
apropriate ownership, it may be necesary sometimes to recreate them
by hand:

	chown -R noaaport:noaaport /var/npemwin
	chown -R noaaport:noaaport /var/run/npemwin
	chown -R noaaport:noaaport /var/log/npemwin
