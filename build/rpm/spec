%description
Noaaport Emwin ByteBlaster server

%prep
%setup
./configure.sh

%build
cd build/rpm
make 

%install
cd build/rpm
make install

%post
@POSTINSTALL@

%postun
@POSTUNINSTALL@

%clean
cd build/rpm
make clean

%files
%defattr(-,root,root)

@PLIST@

%changelog
